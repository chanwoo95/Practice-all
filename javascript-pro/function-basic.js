// 함수 선언문

function getUserInfo(name, age) {
  console.log(`name:${name}, age:${age}`);
}
getUserInfo("chanwoo", 27);

// 함수 표현식

const getColor = (color) => {
  console.log(`my color is ${color}`);
};
getColor("red");
